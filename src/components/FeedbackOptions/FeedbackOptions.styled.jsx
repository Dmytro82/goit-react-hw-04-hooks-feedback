import styled from '@emotion/styled';

export const Container = styled.div``;

export const Button = styled.button`
    min-width: 60px;
    padding: 4px;
    &:not(:last-child) {
        margin: 8px;
    }
`;
